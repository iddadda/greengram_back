<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram.application.feed.UserMapper">

    <select id="findProfileByUserId">
        SELECT a.user_id, a.pic, a.created_at, a.uid, a.nick_name, COUNT(DISTINCT b.feed_id) AS feedCount, COUNT(c.user_id) AS allFeedLikeCount
        FROM user a
        JOIN feed b
        ON a.user_id = b.writer_user_id
        LEFT JOIN feed_like c
        ON b.feed_id = c.feed_id
        WHERE a.user_id = ${profileUserId};

    </select>

</mapper>