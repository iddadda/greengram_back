<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram.application.feedComment.FeedCommentMapper">

    <select id="findAllByFeedIdLimitedTo">
        SELECT a.feed_comment_id, a.comment, b.user_id AS writerUserId, b.uid AS writerUid, b.nick_name AS writerNickName, b.pic AS writerPic
        FROM feed_comment a
        JOIN user b
        ON a.user_id = b.user_id
        WHERE a.feed_id = #{feedId}
        ORDER BY a.feed_comment_id
        LIMIT #{startIdx}, #{size}

    </select>

</mapper>